# 执行记录

## 生产 V1 增强阶段 1 — 数据与日志体系增强 — 2026-06-19

**执行时间**：2026-06-19 02:57（本地）

**分支**：`dev-ysq02`

**创建文件**
- `app/models/knowledge_card_revision.py` — 知识卡片修订建议 ORM
- `app/models/knowledge_duplicate_check_log.py` — 知识重复检测记录 ORM
- `app/repositories/knowledge_card_revision_repository.py` — 修订建议仓储
- `app/repositories/knowledge_duplicate_check_repository.py` — 重复检测记录仓储
- `scripts/prod/init_enhance_stage1_tables.py` — 幂等表结构初始化
- `scripts/prod/check_enhance_stage1_logs.py` — 阶段 1 验收脚本

**修改文件**
- `app/models/question_log.py` — 新增 9 字段 + 5 索引
- `app/models/feedback_log.py` — 新增 7 字段 + 4 索引
- `app/models/knowledge_card.py` — 新增 6 字段 + 2 索引
- `app/models/__init__.py` — 注册新模型
- `app/repositories/question_repository.py` — load_only 补字段 + `list_user_recent_modules`
- `app/repositories/feedback_repository.py` — `list_by_status` / `count_by_status` / `list_by_knowledge_card_id`

**实现内容**
- 为 question_log、feedback_log、knowledge_card 补齐后续增强所需字段与索引
- 新增 knowledge_card_revision、knowledge_duplicate_check_log 两张预留表
- 幂等初始化脚本（information_schema 预判字段/索引）
- 独立验收脚本（字段、表、索引、ORM、Repository 基础查询）

**未实现内容**
- 可信回答、置信度、来源展示业务逻辑
- 知识重复检测流程
- 反馈闭环接口
- 运营看板、多轮追问、30 天常问业务接入

**验收命令**
```powershell
python -m compileall app scripts
python app/db/init_db.py
python scripts/prod/init_stage3_tables.py --env-file "docs/prod/.env"
python scripts/prod/init_stage4_tables.py --env-file "docs/prod/.env"
python scripts/prod/init_stage6_tables.py --env-file "docs/prod/.env"
python scripts/prod/init_enhance_stage1_tables.py --env-file "docs/prod/.env"
python scripts/prod/init_enhance_stage1_tables.py --env-file "docs/prod/.env"
python scripts/prod/check_enhance_stage1_logs.py --env-file "docs/prod/.env"
$env:PYTHONIOENCODING='utf-8'; python scripts/prod/run_all_regression_checks.py --env-file "docs/prod/.env" --fast
```

**自检结果**
- [x] `compileall` PASS
- [x] `init_enhance_stage1_tables.py` 连续两次 PASS（幂等）
- [x] `check_enhance_stage1_logs.py` 全部 PASS
- [x] `run_all_regression_checks.py --fast` PASS（需 `PYTHONIOENCODING=utf-8` 避免 Windows GBK 打印异常）

**需要用户处理事项**
- 口头确认阶段 1 验收后，再决定是否进入增强阶段 2
- Windows 下回归脚本建议设置 `$env:PYTHONIOENCODING='utf-8'` 后执行

---

## 生产 V1 增强阶段 2 — 可信回答 + 置信度 + 来源体系 — 2026-06-19

**执行时间**：2026-06-19（本地）

**分支**：`dev-ysq02`

**创建文件**
- `app/services/trusted_answer_service.py` — 可信回答包装服务
- `scripts/prod/check_enhance_stage2_trusted_answer.py` — 阶段 2 验收脚本

**修改文件**
- `app/agent/ask_state.py` — 新增可信回答相关 state 字段
- `app/agent/nodes.py` — 检索/命中/生成/未命中节点接入可信回答
- `app/services/retrieval_service.py` — 复用 classify_confidence 三分支命中
- `app/services/ask_service.py` — 初始 state 与响应映射扩展
- `app/services/ask_log_service.py` — question_log 写入可信回答字段
- `app/services/ask_run_log_service.py` — AskGraphV2 同步写 question_log
- `app/graphs/ask_graph_v2.py` — 企微模拟路径可信回答包装
- `app/repositories/question_repository.py` — `update_trusted_answer_fields`
- `app/schemas/ask_schema.py` — 可选新增响应字段

**实现内容**
- 高/中置信度答案展示来源、相似度、置信度与反馈提示
- 中置信度增加人工确认提示，低置信度不强答
- `/api/ask` 与 AskGraphV2 路径均回填 question_log 阶段 1 字段

**未实现内容**
- 反馈闭环、重复检测、运营看板、多轮追问、企微卡片按钮

**验收命令**
```powershell
$env:PYTHONIOENCODING="utf-8"
python -m compileall app scripts
python scripts/prod/check_enhance_stage1_logs.py --env-file "docs/prod/.env"
python scripts/prod/check_enhance_stage2_trusted_answer.py --env-file "docs/prod/.env"
python scripts/prod/run_all_regression_checks.py --env-file "docs/prod/.env" --fast
```

**自检结果**
- [x] `compileall` PASS
- [x] `check_enhance_stage1_logs.py` PASS
- [x] `check_enhance_stage2_trusted_answer.py` PASS
- [x] `run_all_regression_checks.py --fast` PASS

**需要用户处理事项**
- 口头确认阶段 2 验收后，再决定是否进入增强阶段 3

---

## 生产 V1 增强阶段 3 — 知识卡片重复检测 — 2026-06-19

**执行时间**：2026-06-19（本地）

**分支**：`dev-ysq02`

**创建文件**
- `app/services/duplicate_check_service.py` — 重复检测核心服务
- `app/schemas/duplicate_check_schema.py` — 检测结果 API 模型
- `scripts/prod/check_enhance_stage3_duplicate_check.py` — 阶段 3 验收脚本

**修改文件**
- `app/services/knowledge_service.py` — submit_audit 接入检测 + check_duplicate
- `app/routers/knowledge_router.py` — POST duplicate-check 接口
- `app/repositories/knowledge_repository.py` — 候选卡片查询
- `app/repositories/knowledge_duplicate_check_repository.py` — create_many
- `app/rag/qdrant_store.py` — search_by_vector 供本地模式检测

**实现内容**
- 提交审核前自动重复检测（只预警不阻断）
- 手动 POST /api/knowledge-cards/{id}/duplicate-check
- 写入 knowledge_duplicate_check_log
- 复用 build_vector_text + Qdrant/Embedding 已有能力

**未实现内容**
- 自动合并、自动阻断、反馈闭环、运营看板
- 企微沉淀直写 pending 路径未接入（绕过 submit_audit）

**验收命令**
```powershell
$env:PYTHONIOENCODING="utf-8"
python scripts/prod/check_enhance_stage1_logs.py --env-file "docs/prod/.env"
python scripts/prod/check_enhance_stage2_trusted_answer.py --env-file "docs/prod/.env"
python scripts/prod/check_enhance_stage3_duplicate_check.py --env-file "docs/prod/.env"
python scripts/prod/run_all_regression_checks.py --env-file "docs/prod/.env" --fast
```

**自检结果**
- [x] 阶段1/2/3 验收 PASS
- [x] fast 回归 PASS

**需要用户处理事项**
- 口头确认阶段 3 验收后，再决定是否进入增强阶段 4

---

## 生产 V1 增强阶段 3 补充 — 企微沉淀路径重复检测 — 2026-06-19

**修改文件**
- `app/graphs/knowledge_deposit_graph.py` — create_pending_knowledge 接入 DuplicateCheckService
- `scripts/prod/check_enhance_stage3_duplicate_check.py` — 增加企微沉淀路径验收

**实现内容**
- 企微沉淀生成 pending 卡片前调用 DuplicateCheckService（source_type=user_deposit）
- 检测失败不进入 pending；检测成功 high_duplicate 只预警
- 沉淀回复话术追加相似知识提示

**验收**
- `check_enhance_stage3_duplicate_check.py` 新增企微路径检查项 PASS

---

## 生产 V1 增强阶段 3 最终补充 — 企微沉淀重复检测闭环 — 2026-06-19

**执行时间**：2026-06-19（本地）

**分支**：`dev-ysq02`

**修改文件**
- `app/graphs/knowledge_deposit_graph.py` — 旧 duplicate_check 改为轻量预检（方案 A）；create_pending_knowledge 改用 `check_card_object_async`
- `app/services/duplicate_check_service.py` — 新增 `check_card_object_async`（修复 LangGraph 异步上下文中 asyncio.run 失败）
- `scripts/prod/check_enhance_stage3_duplicate_check.py` — 企微 mock 全链路验收 + 失败路径验收 + 旧节点职责静态检查

**实现内容**
- 方案 A：旧 `duplicate_check` 节点仅 MySQL/Qdrant 预检并写 `pre_check_only` 标记，不再设置 `duplicate_suspected` 阻断
- 最终重复治理统一在 `create_pending_knowledge` 由 `DuplicateCheckService` 完成并写 `source_type=user_deposit` 日志
- 验收脚本通过 CommandRouter 模拟企微沉淀，验证 pending 成功路径与 duplicate_check_failed 失败路径

**未实现内容**
- 自动合并、高重复自动阻断、反馈闭环、运营看板、多轮追问、30 天记忆

**验收命令**
```powershell
$env:PYTHONIOENCODING="utf-8"
python -m compileall app scripts
python scripts/prod/check_enhance_stage1_logs.py --env-file "docs/prod/.env"
python scripts/prod/check_enhance_stage2_trusted_answer.py --env-file "docs/prod/.env"
python scripts/prod/check_enhance_stage3_duplicate_check.py --env-file "docs/prod/.env"
python scripts/prod/run_all_regression_checks.py --env-file "docs/prod/.env" --fast
```

**自检结果**
- [x] `compileall` PASS
- [x] 阶段1/2/3 验收 PASS
- [x] `run_all_regression_checks.py --fast` PASS（Stage4 补充 AI mock 修复后）

**需要用户处理事项**
- 口头确认阶段 3 最终补充验收后，再决定是否进入增强阶段 4

---

## 生产 V1 增强阶段 3 回归修复 — Stage4 补充 AI 质检 mock — 2026-06-19

**修改文件**
- `scripts/prod/check_stage4_supplement.py` — mock `ai_quality_check`；更新强重复验收项对齐阶段3方案A

**修复内容**
- fast 回归不再依赖真实 LLM JSON 输出，避免 `Expecting ',' delimiter` 波动
- 生产 `KnowledgeDepositCheckService.ai_quality_check` 未改动

**自检结果**
- [x] compileall / 阶段1/2/3 / `run_all_regression_checks.py --fast` 全部 PASS

---

## 生产 V1 增强阶段 4 — 反馈闭环系统 MVP — 2026-06-19

**执行时间**：2026-06-19（本地）

**分支**：`dev-ysq02`

**创建文件**
- `app/services/feedback_evolution_service.py` — 反馈闭环核心服务
- `scripts/prod/check_enhance_stage4_feedback_loop.py` — 阶段 4 验收脚本

**修改文件**
- `app/schemas/feedback_schema.py` — 新增 FeedbackSubmitRequest，保留 legacy 兼容
- `app/routers/feedback_router.py` — POST /api/feedback、GET 列表/详情、/legacy 兼容
- `app/repositories/question_repository.py` — get_latest_feedback_target
- `app/repositories/knowledge_repository.py` — update_feedback_stats
- `app/repositories/feedback_repository.py` — get_by_id
- `app/wecom/command_router.py` — 文本反馈优先识别，不进入 RAG
- `app/graphs/ask_graph_v2.py` — mock wecom 写入 group_id
- `app/services/ask_run_log_service.py` — question_log 回填 group_id
- `app/static/js/ask_test.js` — 反馈改走 /api/feedback/legacy
- `scripts/prod/run_all_regression_checks.py` — 增加 Stage4 反馈闭环步骤

**实现内容**
- 有用 / 无用 / 补充三类反馈，自动绑定最近 30 分钟 question_log
- 更新 knowledge_card 计数与 quality_score，补充生成 knowledge_card_revision
- 企微模拟文本「有用」「无用：」「补充：」不走 RAG

**未实现内容**
- 运营看板、多轮追问、30 天常问、企微卡片按钮、自动审核、自动改正式卡片、Qdrant 同步

**验收命令**
```powershell
$env:PYTHONIOENCODING="utf-8"
python -m compileall app scripts
python scripts/prod/check_enhance_stage1_logs.py --env-file "docs/prod/.env"
python scripts/prod/check_enhance_stage2_trusted_answer.py --env-file "docs/prod/.env"
python scripts/prod/check_enhance_stage3_duplicate_check.py --env-file "docs/prod/.env"
python scripts/prod/check_enhance_stage4_feedback_loop.py --env-file "docs/prod/.env"
python scripts/prod/run_all_regression_checks.py --env-file "docs/prod/.env" --fast
```

**自检结果**
- [x] 阶段1/2/3/4 验收 PASS
- [x] fast 回归 PASS

**需要用户处理事项**
- 口头确认阶段 4 验收后，再决定是否进入下一阶段
