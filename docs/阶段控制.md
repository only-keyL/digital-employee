# 阶段控制

## 当前阶段

阶段3_文档知识库交付总结

## 阶段目标

不新增业务功能、不改核心问答逻辑，只对「文档上传 + 文档切片 + 文档向量同步 + `/api/ask` 文档问答接入」进行交付总结，补齐文档、调用链、验收清单与演示说明。

本阶段完成：

1. 新增交付文档 16～19（RAG 说明、上传切片、问答调用链、验收清单）；
2. 更新 README、总览文档、边界文档索引；
3. 更新阶段控制、总阶段规划、执行记录；
4. 按需更新 `check_enhance_stage7_docs.py` 纳入 16～19 文档检查。

## 阶段1回顾（已完成）

阶段1_文档上传解析与切片入库 — 已验收通过。

- 文档上传、解析、切片、MySQL 入库
- 表：`document_source`、`document_chunk`
- 验收：`check_document_stage1.py`

## 阶段2回顾（已完成）

阶段2_文档向量同步与问答接入 — 已验收通过。

- 文档 chunk 同步 Qdrant
- `/api/ask` 双来源检索（知识卡片优先 + 文档切片补充）
- 验收：`check_document_stage2_rag.py`

## 本阶段禁止事项

1. 不允许新增业务接口。
2. 不允许修改 `/api/ask` 主流程。
3. 不允许修改 `AskGraphV2`。
4. 不允许修改 `RetrievalService` / `RagRetrievalService` 检索逻辑。
5. 不允许修改 Qdrant 同步逻辑。
6. 不允许修改数据库表结构。
7. 不允许新增 LangGraph 节点。
8. 不允许做 LLM 自动生成候选 QA。
9. 不允许做 PDF OCR、复杂表格解析、rerank 等增强能力。
10. 不允许把 API Key、数据库密码、Qdrant Key 写入文档示例。
11. 不允许删除已有交付文档内容。
12. 不允许自动进入后续增强阶段。

## 阶段验收命令

```powershell
cd F:\WorkSpace\digital-employee-assistant
.\.venv\Scripts\Activate.ps1
$env:PYTHONIOENCODING="utf-8"

python scripts/prod/check_document_stage1.py --env-file ".env"
python scripts/prod/check_document_stage2_rag.py --env-file ".env" --fast
python scripts/prod/check_document_stage2_rag.py --env-file ".env"
python scripts/prod/check_rag_v2.py --env-file ".env" --question "测试问题"
python scripts/prod/check_enhance_stage7_docs.py
```

## 阶段完成标准

1. 新增 16～19 四篇文档。
2. README 已增加文档知识库 RAG 入口与索引。
3. 总览文档已补充文档知识库能力。
4. 边界文档已补充文档 RAG 边界与后续优化。
5. 阶段控制、总阶段规划、执行记录已更新。
6. 文档检查脚本通过。
7. 阶段1、阶段2 验收命令仍可通过。
8. 没有新增业务代码。
9. 没有修改核心问答逻辑。
